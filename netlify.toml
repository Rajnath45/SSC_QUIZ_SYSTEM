[build]
  # Netlify deploys the full static repo so /auth and /dashboard are available.
  # The command keeps the legacy CORE/QUIZZES path working for the existing quiz engine.
  command = "mkdir -p CORE/QUIZZES && cp QUIZZES/*.json CORE/QUIZZES/"
  publish = "."
